server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/noorraza/healthcare-config-repo
          # adjust to your local path to healthcare-config-repo
          default-label: master
